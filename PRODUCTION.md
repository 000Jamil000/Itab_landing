# 🚀 Деплой iTAB Landing — Инструкция

## ✅ Проект успешно развёрнут!

### 🌐 URL сайта:
**https://supplier.itab.pro**

---

## 📊 Текущая архитектура на сервере

```
Интернет (88.210.53.64)
   ↓
Nginx (системный, порт 80/443)
   ├─→ supplier-analytics.itab.pro → localhost:3000 (Docker frontend аналитики)
   ├─→ api.supplier-analytics.itab.pro → localhost:8787 (Docker backend аналитики)
   └─→ supplier.itab.pro → localhost:8082 (Docker iTAB Landing) ✅
```

### Используемые порты:
- **3000** — frontend аналитики
- **8787** — backend API аналитики
- **8082** — iTAB Landing

### Nginx конфигурации:
- `/etc/nginx/sites-enabled/supplier-analytics` — аналитика
- `/etc/nginx/sites-enabled/itab-landing` — лендинг

---

## 🔄 Как обновить проект

### Вариант 1: Автоматический деплой (рекомендуется)

```bash
# Локально
git add .
git commit -m "Описание изменений"
git push origin main

# На сервере (через SSH)
ssh root@88.210.53.64
cd /opt/itab-landing
git pull origin main
docker compose up -d --build
```

### Вариант 2: Через деплой-скрипт (в разработке)

```bash
./deploy.sh
```

*(Скрипт требует настройки SSH-ключей)*

---

## 🐳 Docker команды

```bash
# Подключиться к серверу
ssh root@88.210.53.64

# Перейти в директорию проекта
cd /opt/itab-landing

# Посмотреть логи
docker compose logs -f

# Перезапустить контейнер
docker compose restart

# Пересобрать и перезапустить
docker compose up -d --build

# Остановить контейнер
docker compose down

# Посмотреть статус всех контейнеров
docker ps
```

---

## 🌐 Nginx команды

```bash
# Проверить синтаксис конфигурации
nginx -t

# Перезагрузить конфигурацию (без остановки)
systemctl reload nginx

# Рестарт Nginx
systemctl restart nginx

# Посмотреть логи
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# Посмотреть все сайты
ls -la /etc/nginx/sites-enabled/
```

---

## 🔐 Git аутентификация

Проект использует HTTPS с токеном для клонирования.

### Как обновить токен:

```bash
# На сервере
cd /opt/itab-landing
git remote set-url origin https://TOKEN@github.com/000Jamil000/Itab_landing.git
```

Где `TOKEN` — ваш GitHub Personal Access Token.

### Текущий токен (храните в секрете):
```
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
```

---

## 📂 Структура проекта на сервере

```
/opt/itab-landing/
├── .git/                    # Git репозиторий
├── docker-compose.yml       # Конфигурация Docker
├── Dockerfile               # Образ для сборки
├── nginx/                   # Nginx конфиг для контейнера
├── deploy/                  # Конфиги для сервера
│   └── nginx/
│       └── supplier.itab.pro.conf
├── src/                     # Исходный код React
├── public/                  # Статические файлы
└── package.json             # Зависимости Node.js
```

---

## ✅ Проверка работоспособности

```bash
# Проверить, что контейнер запущен
docker ps | grep itab-landing

# Проверить, что порт слушается
netstat -tuln | grep :8082

# Проверить доступность сайта
curl -I https://supplier.itab.pro

# Проверить SSL-сертификат
curl -vI https://supplier.itab.pro 2>&1 | grep -i ssl
```

---

## 🛠️ Решение проблем

### Сайт не открывается (502 Bad Gateway)

```bash
# 1. Проверить Docker
docker ps | grep itab-landing
docker compose logs

# 2. Проверить, что порт доступен
curl http://localhost:8082

# 3. Перезапустить контейнер
docker compose restart
```

### Изменения не применяются

```bash
# Пересобрать образ заново
docker compose up -d --build --force-recreate
```

### Nginx не работает

```bash
# Проверить синтаксис
nginx -t

# Посмотреть логи ошибок
tail -f /var/log/nginx/error.log

# Перезапустить Nginx
systemctl restart nginx
```

---

## 📝 Важные замечания

1. **Не удаляйте SSL-сертификаты** — они автоматически обновляются через certbot
2. **Бэкапы** — делайте резервные копии БД/конфигов перед большими изменениями
3. **Логи** — регулярно проверяйте логи Docker и Nginx на ошибки
4. **Обновления** — держите Docker/Node.js/Nginx в актуальном состоянии

---

## 🎉 Готово!

Проект работает по адресу: **https://supplier.itab.pro**

Для обновлений просто делайте `git push` → `git pull` на сервере → `docker compose up -d --build`

**Удачи! 🚀**

