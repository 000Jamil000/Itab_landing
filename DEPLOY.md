# 🚀 Инструкция по деплою iTAB Landing

## 📋 Быстрый старт

### Автоматический деплой через Git (рекомендуется)

```bash
# 1. Настроить SSH-ключи для доступа к серверу (один раз)
ssh-copy-id root@88.210.53.64

# 2. Запустить деплой
./deploy.sh
```

Скрипт автоматически:
- ✅ Проверит Git-статус и предложит закоммитить изменения
- ✅ Запушит изменения в GitHub
- ✅ Подтянет код на сервер через `git pull`
- ✅ Соберёт и перезапустит Docker контейнер
- ✅ Обновит конфигурацию Nginx
- ✅ Проверит SSL-сертификат

---

## 🎯 Что будет развернуто

- **Домен:** https://supplier.itab.pro
- **Сервер:** 88.210.53.64
- **Директория:** /opt/itab-landing
- **Docker порт:** 8082 (внутренний)
- **Nginx:** reverse proxy на порт 8082
- **Git:** git@github.com:000Jamil000/Itab_landing.git

---

## 🔑 Настройка SSH-ключей (один раз)

### Вариант 1: Скопировать SSH-ключ на сервер

```bash
# Если у вас уже есть SSH-ключ
ssh-copy-id root@88.210.53.64

# Если нет, создайте его
ssh-keygen -t ed25519 -C "your_email@example.com"
ssh-copy-id root@88.210.53.64
```

### Вариант 2: Использовать пароль (не рекомендуется)

```bash
# Установить переменную окружения
export ITAB_SERVER_PASSWORD="your_password"
./deploy.sh
```

---

## 🔧 Ручной деплой (пошагово)

### Шаг 1: Закоммитить и запушить изменения

```bash
git add .
git commit -m "Update landing page"
git push origin main
```

### Шаг 2: Подключиться к серверу и обновить код

```bash
ssh root@88.210.53.64

# Если репозиторий ещё не клонирован
cd /opt
git clone git@github.com:000Jamil000/Itab_landing.git itab-landing

# Или подтянуть изменения
cd /opt/itab-landing
git pull origin main
```

### Шаг 3: Перезапустить Docker

```bash
cd /opt/itab-landing

# Остановить старый контейнер (если есть)
docker compose down

# Собрать и запустить
docker compose up -d --build

# Проверить статус
docker compose ps
docker compose logs -f
```

### Шаг 4: Обновить Nginx (если нужно)

```bash
# На сервере
cd /opt/itab-landing

# Скопировать конфиг
cp deploy/nginx/supplier.itab.pro.conf /etc/nginx/sites-available/itab-landing

# Создать симлинк (если ещё не создан)
ln -sf /etc/nginx/sites-available/itab-landing /etc/nginx/sites-enabled/

# Проверить синтаксис
nginx -t

# Перезагрузить Nginx
systemctl reload nginx
```

---

## 🔍 Проверка работы

### После деплоя проверьте:

```bash
# 1. Docker контейнер запущен
docker ps | grep itab-landing

# 2. Порт 4000 слушается
netstat -tuln | grep :4000

# 3. Nginx работает
systemctl status nginx

# 4. Сайт доступен
curl -I https://supplier.itab.pro
```

### В браузере:

1. Откройте https://supplier.itab.pro
2. Проверьте, что сайт загружается
3. Проверьте SSL (зеленый замок в адресной строке)

---

## 🐛 Решение проблем

### Docker контейнер не запускается

```bash
# Посмотреть логи
docker compose logs

# Проверить, что порт свободен
netstat -tuln | grep :4000

# Пересобрать образ
docker compose up -d --build --force-recreate
```

### Nginx не работает

```bash
# Проверить синтаксис
nginx -t

# Посмотреть логи
tail -f /var/log/nginx/error.log

# Проверить, что конфиг активен
ls -la /etc/nginx/sites-enabled/ | grep itab-landing
```

### Сайт не открывается (502 Bad Gateway)

```bash
# 1. Проверить, что Docker контейнер работает
docker ps | grep itab-landing

# 2. Проверить, что Nginx может подключиться к контейнеру
curl http://localhost:4000

# 3. Перезапустить контейнер
docker compose restart
```

### SSL не работает

```bash
# Повторить получение сертификата
certbot --nginx -d supplier.itab.pro --force-renewal

# Проверить автообновление
systemctl status certbot.timer
```

---

## 📊 Мониторинг

### Логи Docker контейнера

```bash
# В реальном времени
docker compose -f /opt/itab-landing/docker-compose.yml logs -f

# Последние 100 строк
docker compose -f /opt/itab-landing/docker-compose.yml logs --tail=100
```

### Логи Nginx

```bash
# Access логи
tail -f /var/log/nginx/access.log | grep supplier.itab.pro

# Error логи
tail -f /var/log/nginx/error.log
```

### Статистика контейнера

```bash
# Использование ресурсов
docker stats itab-landing

# Информация о контейнере
docker inspect itab-landing
```

---

## 🔄 Обновление проекта

### Быстрое обновление через Git

```bash
# Локально
./deploy.sh
```

### Только обновление кода на сервере (без пересборки Docker)

```bash
ssh root@88.210.53.64 "cd /opt/itab-landing && git pull origin main"
```

### Пересборка Docker после изменений

```bash
ssh root@88.210.53.64 "cd /opt/itab-landing && docker compose up -d --build"
```

### Перезапуск контейнера без пересборки

```bash
ssh root@88.210.53.64 "cd /opt/itab-landing && docker compose restart"
```

---

## 🗑️ Удаление проекта

```bash
ssh root@88.210.53.64

# Остановить контейнер
cd /opt/itab-landing
docker compose down

# Удалить конфиг Nginx
rm /etc/nginx/sites-enabled/itab-landing
rm /etc/nginx/sites-available/itab-landing
systemctl reload nginx

# Удалить файлы проекта
rm -rf /opt/itab-landing

# Удалить SSL сертификат (опционально)
certbot delete --cert-name supplier.itab.pro
```

---

## 📝 Архитектура на сервере

```
Интернет
   ↓
Nginx (системный, порт 80/443)
   ├─→ supplier-analytics.itab.pro → localhost:3000 (Docker frontend)
   ├─→ api.supplier-analytics.itab.pro → localhost:8787 (Docker backend)
   └─→ supplier.itab.pro → localhost:8082 (Docker iTAB Landing) ← НОВЫЙ
```

### Используемые порты:

- **3000** — supplier-analytics (frontend)
- **8787** — api.supplier-analytics (backend)
- **8082** — supplier.itab.pro (iTAB Landing) ← новый проект

### Git Workflow:

```
Локальная машина
   ├─ git commit → git push
   ↓
GitHub (git@github.com:000Jamil000/Itab_landing.git)
   ├─ webhook (опционально)
   ↓
Сервер (/opt/itab-landing)
   ├─ git pull
   ├─ docker compose build
   └─ docker compose up
```

---

## 🔐 Безопасность

### Рекомендации:

1. **Firewall настроен:**
```bash
ufw status
# Должны быть открыты: 22 (SSH), 80 (HTTP), 443 (HTTPS)
```

2. **SSL автообновление работает:**
```bash
systemctl status certbot.timer
```

3. **Регулярные обновления:**
```bash
apt update && apt upgrade -y
```

---

## 📞 Полезные команды

```bash
# Подключиться к серверу
ssh root@88.210.53.64

# Статус всех Docker контейнеров
docker ps

# Статус Nginx
systemctl status nginx

# Перезагрузить Nginx
systemctl reload nginx

# Просмотреть все сайты
ls -la /etc/nginx/sites-enabled/

# Проверить занятые порты
netstat -tuln | grep LISTEN

# Проверить использование диска
df -h
```

---

## ✅ Готово!

После успешного деплоя сайт будет доступен по адресу:

**🌐 https://supplier.itab.pro**

Если возникли проблемы, проверьте раздел "Решение проблем" выше.

