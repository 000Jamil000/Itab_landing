# 🔑 Настройка GitHub Deploy Key для сервера

## ⚠️ ВАЖНО: Выполните эти шаги перед первым деплоем!

SSH-ключ уже создан на сервере и готов к использованию.

---

## 📋 SSH Public Key сервера:

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFiBHhLYl7DCSm6Q5br7Ap4OrHSIUEqeJXl+smN2e8CH server@itab-landing
```

---

## 🚀 Как добавить ключ в GitHub (2 минуты):

### Шаг 1: Откройте настройки Deploy Keys

Перейдите по ссылке:

```
https://github.com/000Jamil000/Itab_landing/settings/keys
```

Или вручную:
1. Откройте репозиторий: https://github.com/000Jamil000/Itab_landing
2. Перейдите в **Settings** (вкладка сверху)
3. В левом меню выберите **Deploy keys**

---

### Шаг 2: Добавьте новый Deploy Key

1. Нажмите кнопку **"Add deploy key"** (зелёная кнопка справа вверху)

2. Заполните форму:

   **Title:**
   ```
   Server itab-landing (88.210.53.64)
   ```

   **Key:** (скопируйте и вставьте)
   ```
   ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFiBHhLYl7DCSm6Q5br7Ap4OrHSIUEqeJXl+smN2e8CH server@itab-landing
   ```

   **☑️ Allow write access:**
   - **ОБЯЗАТЕЛЬНО поставьте галку!**
   - Без этого сервер не сможет делать `git pull`

3. Нажмите **"Add key"**

---

### Шаг 3: Проверьте, что ключ добавлен

После добавления вы увидите ключ в списке Deploy Keys:

```
✅ Server itab-landing (88.210.53.64)
   Read/Write access
   Added just now
```

---

## 🎉 Готово!

После добавления ключа запустите финальный скрипт деплоя:

```bash
./finish-deploy.sh
```

Этот скрипт:
- ✅ Клонирует репозиторий на сервер через SSH
- ✅ Соберёт Docker-образ
- ✅ Запустит контейнер
- ✅ Сайт станет доступен по адресу https://supplier.itab.pro

---

## 🔍 Альтернативный способ (если нужен новый ключ):

Если вы хотите создать новый ключ вручную:

```bash
# На сервере
ssh root@88.210.53.64
cd ~/.ssh
ssh-keygen -t ed25519 -f id_ed25519_github -C "deploy@itab"

# Показать публичный ключ
cat ~/.ssh/id_ed25519_github.pub

# Добавить в SSH config
echo "Host github.com
  IdentityFile ~/.ssh/id_ed25519_github" >> ~/.ssh/config
```

Затем добавьте новый ключ в GitHub по инструкции выше.

---

## ❓ Часто задаваемые вопросы

**Q: Зачем нужен Deploy Key?**

A: Deploy Key позволяет серверу скачивать код из приватного репозитория GitHub через `git clone`/`git pull` без использования паролей или personal access tokens.

**Q: Это безопасно?**

A: Да! Deploy Key даёт доступ только к одному репозиторию и может быть в любой момент отозван в настройках GitHub.

**Q: Можно ли использовать Personal Access Token вместо Deploy Key?**

A: Да, но Deploy Key — более безопасный вариант, так как ограничен одним репозиторием.

**Q: Что делать, если ключ скомпрометирован?**

A: Удалите его из GitHub (Settings → Deploy keys → Delete) и создайте новый на сервере.

---

## 📝 Следующие шаги

1. ✅ Добавьте Deploy Key в GitHub (см. инструкцию выше)
2. ✅ Запустите `./finish-deploy.sh`
3. ✅ Проверьте сайт: https://supplier.itab.pro

**Удачи! 🚀**

